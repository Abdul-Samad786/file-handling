#include <fstream>
#include <iostream>
using namespace std;
int main() {
  string line;
  ifstream fin;
  fin.open("hello.txt");
  while(fin){
    getline(fin,line);
    cout<<line<<endl;
  }
  return 0;
}
